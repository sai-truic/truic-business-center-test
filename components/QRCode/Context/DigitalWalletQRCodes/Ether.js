import DigitalWalletsInput from './DigitalWalletsInput';

export const Ether = ({handleInputChange}) => {
  return <DigitalWalletsInput type="Ether" handleInputChange={handleInputChange} />;
};

export default Ether;
