import DigitalWalletsInput from './DigitalWalletsInput';

export const Bitcoin = ({handleInputChange}) => {
  return <DigitalWalletsInput type="Bitcoin" handleInputChange={handleInputChange} />;
};

export default Bitcoin;
