import React from 'react';
import OperatingAgreement from './OperatingAgreement';

const AgreementPage: React.FC = () => {
  // In Next.js, routing is handled by the pages directory structure
  // This component can just render the OperatingAgreement directly
  return <OperatingAgreement />;
};

export default AgreementPage;